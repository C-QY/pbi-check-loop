# Runtime artifacts produced by the tools.
# These contain local project paths and window titles — never commit them.
pbi-reload.last.json
pbi-reload.dialogs.log

# Captured screenshots
*.png
!docs/**/*.png

# Windows / editor noise
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~
